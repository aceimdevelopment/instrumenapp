%h1 
  %i.fa.fa-language
  Idiomas

%table.table.table-striped.table-sm.table-hover
  %thead
    %tr
      %th Id
      %th Descripción
      %th
      %th

  %tbody
    - @languages.each do |language|
      %tr
        %td= language.id
        %td= language.description
        %td
          = link_to edit_language_path(language) do
            %i.fa.fa-pencil
        %td
          - total = language.evaluations.count
          - if total > 0
            %b.tooltip-btn.tooltip-demo{ 'data_toggle': :tooltip, title: 'Total Cursos y/o Pruebas'}
              = total
          - else
            = link_to language, method: :delete, data: { confirm: 'Esta acción es irrevarsible, ¿Está seguro?' }, class: 'text-danger' do
              %i.fa.fa-trash
%br

= link_to new_language_path, class: 'btn btn-sm btn-primary' do
  %i.fa.fa-plus
  Nuevo Idioma
