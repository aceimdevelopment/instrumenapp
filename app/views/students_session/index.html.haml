.border.text-center.p-1.m-1

  %h3.text-center Ofertas:
  =# inscriptions_buttons current_user.id
  = render '/evaluations/evaluations_links'

= render partial: '/inscriptions/index', locals: {inscriptions: current_user.inscriptions}
